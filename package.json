{
  "name": "metadata-generate-script",
  "version": "1.0.0",
  "description": "generates nft metadata",
  "main": "generate-metadata.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "generate": "node generate-metadata.js"
  },
  "author": "justinw",
  "license": "ISC",
  "dependencies": {
    "koilib": "^5.6.1"
  }
}
